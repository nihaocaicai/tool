<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.tool.api.dao.ArrangementDao">
	<!--根据user_id查询用户信息 -->
	<select id="findArrangeByUserId" parameterType="String"
		resultType="com.tool.api.entity.Arrangement">
		select * from Arrangement where user_id=#{user_id}
	</select>
	
	<!-- 根据arrange_id来查询用户信息 -->
	<select id = "findArrangeByArrangeId" parameterType = "String" resultType = "com.tool.api.entity.Arrangement">
    	select * from Arrangement where arrange_id = #{arrange_id}
    </select>

	<!-- 插入一条用户考试安排 -->
	<insert id="insertArrange" parameterType="com.tool.api.entity.Arrangement">
		insert into Arrangement(user_id, arrange_content, arrange_place, arrange_time, arrange_if_prompt, arrange_if_prompt_time)
		values (#{user_id}, #{arrange_content}, #{arrange_place}, #{arrange_time}, #{arrange_if_prompt}, #{arrange_if_prompt_time})
	</insert>
	
	<!-- 更新某用户考试安排 -->
	<update id = "updateArrange" parameterType = "com.tool.api.entity.Arrangement">
    	update Arrangement
    		set arrange_content = #{arrange_content}, arrange_place = #{arrange_place}, arrange_time = #{arrange_time}, arrange_if_prompt = #{arrange_if_prompt}, arrange_if_prompt_time = #{arrange_if_prompt_time}
    			where arrange_id = #{arrange_id}
    </update>

	<!-- 删除某用户考试安排-->
	<delete id="deleteArrange" parameterType="String">
		delete from Arrangement where arrange_id = #{arrange_id}
	</delete>
</mapper>